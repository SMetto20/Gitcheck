import { GoodDirective } from './good.directive';

describe('GoodDirective', () => {
  it('should create an instance', () => {
    const directive = new GoodDirective();
    expect(directive).toBeTruthy();
  });
});
